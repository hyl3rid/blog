const size = {
  xs: "320px",
  sm: "576px",
  md: "735px",
  lg: "1049px",
};

const device = {
  xs: `max-width: ${size.xs}`,
  sm: `max-width: ${size.sm}`,
  md: `max-width: ${size.md}`,
  lg: `max-width: ${size.lg}`,
};

export { device };
